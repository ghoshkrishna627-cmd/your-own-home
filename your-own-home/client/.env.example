# Base URL of the backend API (include /api suffix)
VITE_API_URL=http://localhost:5000/api

# Stripe publishable key (safe to expose client-side — this is NOT the secret key)
# YOU NEED TO DO THIS: https://dashboard.stripe.com/test/apikeys
VITE_STRIPE_PUBLISHABLE_KEY=
